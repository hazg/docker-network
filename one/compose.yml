services:
  one:
    build: ./
    container_name: one
    networks:
      - nginx_network      # proxy and app must be in same network
    volumes:
      - ./src:/app
networks:
  nginx_network:
    external: true